<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tistory.comfy91.sopt26_android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerView.RecyclerViewActivity"></activity>
        <activity android:name=".ViewAndViewContainer.ConstraintDimensionRatioActivity" />
        <activity
            android:name=".BottomNavigationBar.BottomNavigationActivity"
            android:exported="true" />
        <activity android:name=".ViewAndViewContainer.RelativeLoginActivity" />
        <activity android:name=".ViewAndViewContainer.SignUpActivity" />
        <activity android:name=".ViewAndViewContainer.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>